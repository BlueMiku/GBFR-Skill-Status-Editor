# Python bytecode cache
__pycache__/
*.pyc

# PyInstaller build artifacts
build/
dist/
*.spec

# Working cache/edit database + changelog (generated at runtime, not source)
data/cache/

# Staged release zips - built artifacts, not source
release_staging/
